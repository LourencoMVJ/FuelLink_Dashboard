# Composer-managed dependencies — reproducible via composer.lock, never committed
vendor/

# Real secrets — never commit
private/config/env.php
private/config/.env

# Runtime storage (JWKS cache, uploaded proof files, etc.) — never commit
private/storage/

# PHPUnit
.phpunit.cache/
.phpunit.result.cache

# OS / editor junk
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Sprint Documentation & Notes
FASE_1_FRONTEND.md
FASE_2_FRONTEND.md
Fase 1.md
Fase 2.md
Fase*.md
FASE*.md

bruno/

